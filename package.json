{
  "name": "typescript-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "tsc",
    "test": "npx testcafe chrome dist/*.js",
    "clean:linux": "rm -r dist",
    "clean:win": "rmdir /s /q dist",
    "test:linux": "npm run compile && npm run test && npm run clean:linux",
    "test:win": "npm run compile && npm run test && npm run clean:win"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "testcafe": "^3.6.0",
    "typescript": "^5.4.5"
  }
}
